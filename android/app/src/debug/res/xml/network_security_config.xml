<?xml version="1.0" encoding="utf-8"?>
<!-- Debug only: allow HTTP to host machine (emulator 10.0.2.2, LAN IP, localhost). -->
<network-security-config>
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
