import '../shared/account_profile_screen.dart';

/// Creator (Superstar) account + sign out — same as customer profile.
class CreatorProfileScreen extends AccountProfileScreen {
  const CreatorProfileScreen({super.key}) : super(showBack: false);
}
